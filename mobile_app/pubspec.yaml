name: mobile_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  
  # UI & Utilities
  cupertino_icons: ^1.0.2
  google_fonts: ^6.3.3  # Untuk font keren
  intl: ^0.20.2         # Untuk format mata uang (Gaji Guru)
  firebase_storage: ^13.0.5
  file_picker: ^10.3.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true